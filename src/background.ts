import OBR from "@owlbear-rodeo/sdk";

void OBR.onReady(async () => {
  // Cinematic execution is handled by cinematic.ts on every connected client.
  // The room metadata is the shared source of truth; only the GM writes it.
});
